#include <iostream>
#include <string>
#include <system_error>

int main(){
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    
    std::string stock_id,trader_id;
    bool trade;
    double price,volume;

    long long n;
    while(n--){
        std::cin >> stock_id >> trade >> price >> volume >> trader_id;
        
    }
}